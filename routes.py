default_application = "bootup"
default_controller = "project"
default_function = "index"

routers = dict(
    BASE = dict(
        default_application = "bootup",
        default_controller = "project",
        default_function = "index"
        )
)